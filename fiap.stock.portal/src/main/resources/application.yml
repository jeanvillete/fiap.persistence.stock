server:
  port: 8383

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: fiapSampleLogin
  main:
    banner-mode: console

logging:
  config: classpath:logback.xml

stock:
  mgnt:
    host: http://localhost:8282/